import React from "react";
import { CheckCircle } from "lucide-react";

export const IconCheck = ({ className }: { className?: string }) => (
  <CheckCircle className={className} size={16} strokeWidth={2} />
);
